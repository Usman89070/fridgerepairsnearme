# Uploaded images only — never execute scripts from this folder,
# even if something with a script extension ends up in here.
<FilesMatch "\.(php|phtml|php\d|pht|cgi|pl|py|sh)$">
  Require all denied
</FilesMatch>
Options -Indexes
